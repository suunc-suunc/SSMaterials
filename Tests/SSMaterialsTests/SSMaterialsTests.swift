import XCTest
@testable import SSMaterials

final class SSMaterialsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SSMaterials().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
